import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'PropertyPulse | Add Property',
};

export default function PropertyAddPage() {
  return <div>Property Add</div>;
}
